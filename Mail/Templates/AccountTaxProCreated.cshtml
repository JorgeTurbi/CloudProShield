@model dynamic
@{
    ViewBag.Title = "Cuenta creada automáticamente";
    Layout = "Layout";
}

<h2>¡Tu cuenta en @Model.AppName está lista!</h2>

<p>Hola,</p>
<p>
    Has sido registrado automáticamente en <strong>@Model.AppName</strong>.
    A continuación encontrarás tu contraseña temporal:
</p>

<div class="otp-code">@Model.Password</div>

<p>
    Te recomendamos iniciar sesión y cambiarla cuanto antes.
</p>

<div style="text-align:center">
    <a href="@Model.LoginUrl" class="button">Iniciar sesión</a>
</div>

<div class="info-box">
    <p>Esta contraseña es válida hasta que la cambies manualmente desde tu perfil.</p>
</div>

<div class="signature">
    <p>Saludos,<br />El equipo de @Model.AppName</p>
</div>